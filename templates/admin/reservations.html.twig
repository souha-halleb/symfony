{% extends 'base.html.twig' %}
{# FIX #16 : Ce template était manquant → la route admin_reservations lançait une Twig exception #}
{% block title %}Toutes les réservations — Admin{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="fw-bold mb-1">
                <i class="bi bi-ticket-perforated me-2 text-success"></i>Toutes les réservations
            </h1>
            <p class="text-muted mb-0">{{ reservations|length }} réservation(s) au total</p>
        </div>
        <a href="{{ path('admin_dashboard') }}" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i>Retour
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Email</th>
                        <th>Téléphone</th>
                        <th>Événement</th>
                        <th>Date réservation</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for reservation in reservations %}
                    <tr>
                        <td class="text-muted small">{{ reservation.id }}</td>
                        <td class="fw-semibold">{{ reservation.name }}</td>
                        <td>{{ reservation.email }}</td>
                        <td>{{ reservation.phone }}</td>
                        <td>
                            {% if reservation.event %}
                                <span class="badge bg-primary">{{ reservation.event.title }}</span>
                            {% else %}
                                <span class="text-muted">N/A</span>
                            {% endif %}
                        </td>
                        <td class="text-muted small">{{ reservation.createdAt|date('d/m/Y H:i') }}</td>
                        <td class="text-center">
                            <a href="{{ path('admin_user_edit', {'id': reservation.id}) }}" class="btn btn-sm btn-outline-warning">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form method="post" action="{{ path('admin_user_delete', {'id': reservation.id}) }}"
                                  class="d-inline" onsubmit="return confirm('Supprimer cette réservation ?')">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
                                <button type="submit" class="btn btn-sm btn-outline-danger">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="7" class="text-center text-muted py-4">
                            <i class="bi bi-inbox fs-4 d-block mb-2"></i>Aucune réservation trouvée
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
